 // for( int i = 0; i < wordsByLength.length; i++ )
        //     wordsByLength[ i ] = new ArrayList<>( );
        
        // for( int i = 0; i < BUCKETS; i++ )
        //     buckets[ i ] = new ArrayList<>( );

        //     //for( int i = 0; i < 4; i++ )
        // //System.out.println(wordsByLength.length);
        
        // for( String s : arr )
        //     wordsByLength[ s.length( ) ].add( s );
